# impit: impit/src/lib.rs
#
# Against https://github.com/apify/impit at 4fd6c3167c55d9d059a3e5872846e0b5c0a31e3b.
# Generated by scripts/vendor-diff.ps1 from vendor/impit. Do not edit:
# the vendored tree is the truth and this is derived from it. What the
# change is for is patches/UPSTREAM.md.
diff --git a/impit/src/lib.rs b/impit/src/lib.rs
index 0af8cae..9dcbc6f 100644
--- a/impit/src/lib.rs
+++ b/impit/src/lib.rs
@@ -52,17 +52,11 @@
 //! ```
 //!
 //! Without the patched dependencies, the project won't build.
-//!
-//! Note that you also have to build your project with `rustflags = "--cfg reqwest_unstable"`, otherwise, the build will also fail.
-//! This is because `impit` uses unstable features of `reqwest` (namely `http3` support), which are not available in the stable version of the library.
 
 #![deny(unused_crate_dependencies)]
 mod http_headers;
-mod response_parsing;
 mod tls;
 
-pub(crate) mod http3;
-
 /// Main module that contains the `Impit` struct and its methods.
 pub mod impit;
 
@@ -75,14 +69,6 @@ pub mod errors;
 /// Browser fingerprint definitions and types.
 pub mod fingerprint;
 
-/// Various utility functions and types.
-pub mod utils {
-    pub use crate::response_parsing::decode;
-    pub use crate::response_parsing::determine_encoding;
-    pub use crate::response_parsing::ContentType;
-    pub use encoding::all as encodings;
-}
-
 /// Reexports reqwest cookie-related types and functions.
 /// This module is used to provide a default cookie store for the `Impit` struct.
 pub mod cookie {
